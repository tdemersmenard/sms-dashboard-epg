"use client";

// No longer needed — franchise context is now URL-based via /[slug]/ routes.
// Kept as empty export to avoid breaking any lingering imports.
export default function ImpersonationBanner() {
  return null;
}
